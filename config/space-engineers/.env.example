# Space Engineers module-specific environment variables
# Copy to config/space-engineers/.env and adjust values

# Ingest-only mode (auto registration)
# Allow unknown servers to auto-register on first health ingest
SE_AUTO_REGISTER_SERVERS=true
# Optional API key required in health payload meta.apiKey
SE_INGEST_API_KEY=change-me-strong-key
