# 🌍 Environment Configuration Template
# Copy this file to .env and fill in your actual values

# ===========================================
# 🗄️ DATABASE CONFIGURATION
# ===========================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=game_hub_db
DB_SSL=false

# ===========================================
# 🔐 JWT AUTHENTICATION
# ===========================================
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production
JWT_EXPIRES_IN=7d

# ===========================================
# 🎮 STEAM AUTHENTICATION
# ===========================================
STEAM_API_KEY=your_steam_api_key_here
RETURN_URL=http://localhost:4000/auth/steam/return
REALM=http://localhost:4000/

# ===========================================
# 🌐 SERVER CONFIGURATION
# ===========================================
NODE_ENV=development
PORT=4000
HOST=0.0.0.0

# Domain configuration
DOMAIN=localhost:4000
BASE_URL=http://localhost:4000

# ===========================================
# 🔧 CORS CONFIGURATION
# ===========================================
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ===========================================
# 📝 LOGGING CONFIGURATION
# ===========================================
LOG_LEVEL=debug
LOG_FILE=./logs/app.log
ERROR_LOG_FILE=./logs/error.log

# ===========================================
# 🚀 PRODUCTION SPECIFIC (override in .env.production)
# ===========================================
# NODE_ENV=production
# DB_SSL=true
# LOG_LEVEL=info
# DOMAIN=api.yourdomain.com
# BASE_URL=https://api.yourdomain.com
# RETURN_URL=https://api.yourdomain.com/auth/steam/return
# REALM=https://api.yourdomain.com/
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ===========================================
# 💰 WALLET SYSTEM
# ===========================================
# Default currency settings
DEFAULT_CURRENCY_CODE=CREDITS
INITIAL_WALLET_BALANCE=1000

# ===========================================
# 🎮 GAME SERVERS
# ===========================================
# Space Engineers Ingest
# Enable auto-registration of servers reporting health
SE_AUTO_REGISTER_SERVERS=true
# Optional API key required in health payload meta.apiKey
SE_INGEST_API_KEY=change-me-strong-key

# Valheim Server
VALHEIM_SERVER_HOST=localhost
VALHEIM_SERVER_PORT=2456

# ===========================================
# 📧 NOTIFICATION SETTINGS (Optional)
# ===========================================
# Email configuration (for future use)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# ===========================================
# 🔍 MONITORING & ANALYTICS (Optional)
# ===========================================
# SENTRY_DSN=your_sentry_dsn_here
# GOOGLE_ANALYTICS_ID=your_ga_id_here

# ===========================================
# 🪣 S3 / AWS (Space Engineers Hangar)
# ===========================================
# Required for Hangar S3 uploads/downloads
# Prefer the SE_* keys; S3_BUCKET/AWS_REGION are supported as fallbacks
SE_HANGAR_S3_BUCKET=your_bucket_name
SE_HANGAR_S3_REGION=ap-northeast-2
# Or use generic names instead of the above two:
# S3_BUCKET=your_bucket_name
# AWS_REGION=ap-northeast-2

# Credentials (skip if using IAM roles/instance profiles)
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key

# ===========================================
# 🛠️ Space Engineers single-server fallback
# ===========================================
# Used when config/space-engineers/servers.yaml is not present
SE_SERVER_HOST=localhost
SE_SERVER_PORT=27016
# Optional tuning
SE_DROP_RATE_MULTIPLIER=1
SE_MARKETPLACE_ENABLED=false
SE_MAX_STORAGE_SLOTS=100
SE_MAX_ITEM_STACK=100
# Optional config path / default id if you maintain a registry file
# SE_SERVERS_CONFIG=config/space-engineers/servers.yaml
# SE_DEFAULT_SERVER_ID=default

# ===========================================
# 🧩 Docker Compose compatibility (Postgres)
# ===========================================
# docker-compose.yml uses these names for the DB container; they mirror DB_USER/DB_NAME
DB_USERNAME=your_db_user
DB_DATABASE=game_hub_db
